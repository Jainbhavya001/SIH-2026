# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

# --- Imaging / CV ---
opencv-python-headless==4.10.0.84
Pillow
numpy

# --- OCR (choose one) ---
# PaddleOCR — modern, layout-aware OCR; better on structured docs with
# holograms/MRZ.  Installs paddlepaddle + paddlepaddleocr.
#   pip install paddlepaddle paddlepaddleocr
# Tesseract — classic OCR; works if the Tesseract binary is on PATH.
#   pip install pytesseract
#   • On Windows: apt-get install tesseract-ocr or download the binary
#     from https://github.com/UB-Mannheim/tesseract/wiki
#   • On macOS: brew install tesseract
#   • On Linux: sudo apt-get install tesseract-ocr
pytesseract==0.3.13
# NOTE: pytesseract is a wrapper. You must also install the Tesseract-OCR
# binary separately (see README "OCR setup" section) — apt/brew/choco
# package, not a pip package.
#
# For PaddleOCR:
#   pip install paddlepaddle paddlepaddleocr
#   python -c "from paddleocr import PaddleOCR; ocr = PaddleOCR(lang='en', use_angle_cls=True, show_log=False)"

# --- Optional, ArcFace face verification (recommended — best accuracy) ---
# insightface uses RetinaFace detection + ArcFace 512-d embeddings.
# Models (~300 MB) are auto-downloaded on first use to ~/.insightface/models/.
#   pip install insightface onnxruntime
# insightface>=0.7
# onnxruntime>=1.16

# --- Optional, dlib face verification (alternative to ArcFace) ---
# face_recognition==1.3.0      # needs CMake + dlib; heavier install on Windows

# --- Optional, Faster R-CNN document region localization (Module 0) ---
# Without these (or without a weights file) localization falls back to the
# OpenCV heuristic detector, so the pipeline and tests still run on CPU.
# Put trained weights at backend/models/localization/fasterrcnn_document_regions.pth
# or set LOCALIZATION_MODEL_PATH. CPU wheels:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# torch>=2.2
# torchvision>=0.17

# --- Testing ---
pytest==8.3.3
httpx==0.27.2
